import { Box } from "@mui/material";
import React from "react";

const BottomHeader = () => {
  return (
    <Box sx={{boxShadow: theme => theme.shadows.header}}>
      BottomHeader
    </Box>
  );
};

export default BottomHeader;
